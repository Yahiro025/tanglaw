# Plan: Restyle Five Pillars to match Landas carousel

## Goal
Replace the current single-card-at-a-time display with a Landas-style horizontal snap-scroll carousel, matching its visual design (gradient numbers, icons, card styling, active/inactive states).

## Reference
Landas `PillarCarousel` from `src/components/AboutView.jsx` — horizontal scroll with `w-[36rem]` cards, snap points, `px-[calc(50%-18rem)]` centering, active card `opacity-100 scale-100` vs inactive `opacity-50 scale-[0.95]`.

## File to modify
`frontend/src/app/about/page.tsx` — `CarouselSection` component (lines ~192-330)

## Changes

### 1. Add pillar icons array
Add a `PILLAR_ICONS` array with SVG icons for each pillar (matching Landas pattern — simple lucide-react icons or inline SVGs).

### 2. Rewrite CarouselSection to horizontal scroll carousel
- Restore `carouselRef`, scroll-based `handleScroll`, `getCardWidth`, `scrollTo`
- Container: `overflow-x-auto snap-x snap-mandatory` with horizontal padding to center cards
- Cards: `w-[36rem]` (576px), `snap-center`, active card `opacity-100 scale-100`, inactive `opacity-50 scale-[0.95]`
- Each card renders: gradient number + icon box, title, justified description
- Keep autoplay, prev/next arrows, indicator dots

### 3. Card layout (per Landas)
```
┌──────────────────────────────────┐
│  [01]  [icon box]                │
│                                  │
│  Title                           │
│                                  │
│  Description text (justified)    │
│                                  │
└──────────────────────────────────┘
```

## Verification
- `npm run dev`, navigate to `/about`
- Scroll horizontally through 5 pillars
- Active card is full opacity/scale, neighbors are dimmed
- Arrows and dots navigate correctly
- Autoplay advances every 4s
- Works on both mobile and desktop
