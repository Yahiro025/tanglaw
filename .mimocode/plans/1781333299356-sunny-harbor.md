# Plan: Fix navbar overlap + add footer links to landing page

## Issues
1. **Navbar overlaps content** on about, contact, and home pages — the fixed navbar covers the top of page content because pages don't have enough top padding.
2. **Missing bottom section** on landing page — user wants HOME, ABOUT, CONTACT links visible at the bottom.

## Files to modify

### 1. `src/app/about/page.tsx` (line 382)
Change `py-20` → `pt-28 pb-20` to push content below the fixed navbar (~80px).

### 2. `src/app/contact/page.tsx` (line 43)
Change `py-16` → `pt-24 pb-16` to push content below the fixed navbar.

### 3. `src/app/home-client.tsx` (line 31)
Change `py-20` → `pt-24 pb-20` to push hero content below the fixed navbar.

### 4. `src/components/site-footer.tsx`
Add HOME, ABOUT, CONTACT navigation links to the footer so they appear on all public pages including the landing page.

## Verification
- Start dev server, check `/` — hero content not hidden behind navbar, footer visible with nav links
- Check `/about` — "Who Builds TANGLAW" header not overlapped
- Check `/contact` — "Contact TANGLAW" header not overlapped
- Check footer on all pages has HOME, ABOUT, CONTACT links
