# Plan: Restore LinkedIn logo on team profile hover

## Problem
The about page team cards currently show a generic `ExternalLink` icon (lucide-react) on hover. The user wants the actual LinkedIn logo/icon instead.

## File to modify
`frontend/src/app/about/page.tsx`

## Changes

### 1. Update import (line 7)
Change:
```ts
import { Users, BookOpen, ExternalLink, ChevronLeft, ChevronRight } from "lucide-react";
```
To:
```ts
import { Users, BookOpen, Linkedin, ChevronLeft, ChevronRight } from "lucide-react";
```

### 2. Replace icon in Documentation Team cards (line 526)
Change:
```tsx
<ExternalLink className="h-5 w-5 text-white" />
```
To:
```tsx
<Linkedin className="h-5 w-5 text-white" />
```

### 3. Replace icon in Development Team cards (line 572)
Same change as above.

## Verification
- Run `npm run dev` in the frontend directory
- Navigate to `/about`
- Hover over any team member's photo
- Confirm the LinkedIn logo appears (not a generic link icon)
- Click the icon and verify it opens the correct LinkedIn profile in a new tab
